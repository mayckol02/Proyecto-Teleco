spring.application.name=auth-service
server.port=8082

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=3600000

# Logging
logging.level.teleco.auth=INFO
logging.level.org.springframework.security=INFO
